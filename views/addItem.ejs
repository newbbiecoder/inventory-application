<html>
    <head>
        <title><%= title %></title>
        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <%- include('templates/header') %>
        <div class="container">
            <%- include("templates/navbar", {onDash: onDash}) %>
            <main>
                <section class="add-item-header">
                    <h1><%= title %></h1>
                    <p>Use this form to add products to the inventory.</p>
                </section>
                <div class="form-container">
                    <form action="/<%= category %>/new" method="POST">
                        <h3>Item Details</h3>
                        <div class="itemNameContainer">
                            <label for="itemName">Item Name</label>
                            <input type="text" name="itemName" id="itemName" required>
                        </div>

                        <div class="itemCategoryContainer">
                            <label for="itemCategory">Item Category</label>
                            <select name="itemCategory" id="itemCategory" disabled>
                                <option value="vegetables">Vegetables</option>
                                <option value="fruits">Fruits</option>
                                <option value="dairy">Dairy</option>
                                <option value="grains">Grains</option>
                                <option value="snacks">Snacks</option>
                                <option value="beverages">Beverages</option>
                            </select>
                            <% if(category === "vegetables") { %>
                                <p>Usually measured in kilograms or grams. Pieces for individual items like corn.</p>
                            <% } %>
                            <% if(category === "fruits") { %>
                                <p>Commonly tracked in kilograms or pieces. Use dozens for bulk fruits.</p>
                            <% } %>
                            <% if(category === "dairy") { %>
                                <p>Typically measured in liters or milliliters. Pieces for packaged items.</p>
                            <% } %>
                            <% if(category === "grains") { %>
                                <p>Best measured in kilograms or bags. Grams for small quantities.</p>
                            <% } %>
                            <% if(category === "snacks") { %>
                                <p>Usually counted in pieces or packs. Boxes for bulk packaging.</p>
                            <% } %>
                            <% if(category === "beverages") { %>
                                <p>Measured in liters or milliliters. Bottles or cans for packaged drinks.</p>
                            <% } %>
                        </div>

                        <div class="quantity-unit-container">
                            <div>
                                <label for="quantity">Quantity</label>
                                <input type="number" name="quantity" id="quantity" required>
                            </div>
                            <div>
                                <label for="unit">Unit</label>
                                <% if(category === "vegetables") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="kg">kg</option>
                                        <option value="bags">bags</option>
                                        <option value="heads">heads</option>
                                        <option value="pcs">pcs</option>
                                        <option value="bunch">bunch</option>
                                    </select>
                                <% } %>
                                <% if(category === "fruits") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="kg">kg</option>
                                        <option value="pcs">pcs</option>
                                        <option value="dozen">dozen</option>
                                    </select>
                                <% } %>
                                <% if(category === "dairy") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="liters">liters</option>
                                        <option value="cups">cups</option>
                                        <option value="packs">packs</option>
                                        <option value="blocks">blocks</option>
                                        <option value="cartons">cartons</option>
                                    </select>
                                <% } %>
                                <% if(category === "grains") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="kg">kg</option>
                                        <option value="bags">bags</option>
                                        <option value="packs">packs</option>
                                    </select>
                                <% } %>
                                <% if(category === "snacks") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="packs">packs</option>
                                        <option value="pcs">pcs</option>
                                    </select>
                                <% } %>
                                <% if(category === "beverages") { %>
                                    <select name="unit" id="unit" required>
                                        <option value="liters">liters</option>
                                        <option value="bottles">bottles</option>
                                        <option value="cans">cans</option>
                                    </select>
                                <% } %>
                            </div>
                        </div>

                        <div class="expiryDateContainer">
                            <label for="expiryDate">Expiry Date (Optional)</label>
                            <input type="date" name="expiryDate" id="expiryDate">
                        </div>

                        <div class="submit-cancel-button">
                            <button type="submit">Add Item</button>
                            <a class="cancelButton" onclick="window.location.href='/categories/<%= category %>'">Cancel</a>
                        </div>
                    </form>

                    <div class="quick-tips">
                        <h3>Quick Tips</h3>
                        <ul>
                            <li><b>Category first:</b> Select the correct category before entering quantity and unit.</li>
                            <li><b>Preferred units:</b> Use kg or pcs for produce, liters or ml for liquids.</li>
                            <li><b>Expiry awareness:</b> Add expiry dates for perishable items to track freshness.</li>
                            <li><b>Accurate quantities: </b>Enter realistic stock amounts to avoid false low-stock alerts.</li>
                            <li><b>Accurate quantities:</b> Enter realistic stock amounts to avoid false low-stock alerts.</li>
                        </ul>
                    </div>
                </div>
            </main>
        </div>
        <footer>
            <p class="footer-designer">
                © 2026 FreshStock. All rights reserved.
                <span class="bullet-seperator">•</span>
                Designed by
                <a href="https://github.com/newbbiecoder" target="_blank">newbbiecoder (GitHub)</a>
            </p>
        </footer>
    </body>
</html>